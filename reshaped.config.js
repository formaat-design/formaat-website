// @ts-check
/** @type {import('reshaped').ReshapedConfig} */
const config = {
  themes: {
    formaat: {
      fontFamily: {
        display: {
          family: "Work Sans, Arial, sans-serif",
        },
        // body: {
        //   family: "Work Sans, Arial, sans-serif",
        // },
      },
    },
  },
};

module.exports = config;
