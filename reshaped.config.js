// @ts-check
/** @type {import('reshaped').ReshapedConfig} */
const config = {
  themes: {
    formaat: {
      fontFamily: {
        display: {
          family: "Basier, Arial, sans-serif",
        },
        body: {
          family: "Basier, Arial, sans-serif",
        },
      },
    },
  },
};

module.exports = config;
