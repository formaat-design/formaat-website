// @ts-check
/** @type {import('reshaped').ReshapedConfig} */
const config = {
  themes: {
    formaat: {
      fontFamily: {
        display: {
          family: "Cabin, Arial, sans-serif",
        },
      },
    },
  },
};

module.exports = config;
