// @ts-check
/** @type {import('reshaped').ReshapedConfig} */
const config = {
  themes: {
    formaat: {
      fontFamily: {
        display: {
          family: "Outfit, Arial, sans-serif",
        },
      },
    },
  },
};

module.exports = config;
